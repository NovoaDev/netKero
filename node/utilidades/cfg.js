let config = {
  key: {
    sqlServer: "",
    sqlUser: process.env."",
    sqlPassword: process.env."",
    sqlDatabase: process.env."",
    keyVal1: process.env."",
    keyVal2: process.env."",
    keyVal3: process.env.""
  }
}

module.exports = config